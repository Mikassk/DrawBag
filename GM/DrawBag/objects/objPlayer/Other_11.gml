/// @description key input

keyup = -keyboard_check(keyboard.up);
keydown = keyboard_check(keyboard.down);
keyleft = -keyboard_check(keyboard.left);
keyright = keyboard_check(keyboard.right);
keyatk = keyboard_check_pressed(keyboard.atk);
